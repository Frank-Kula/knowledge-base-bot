# 为什么 Windows 安装新版本后打开还是旧版本？

可能是更新时安装路径与旧版本安装路径不同导致的，实际同时安装了两个版本。以下是解决方案：
1. 确保安装路径一致
检查旧版本路径：右键桌面 Apifox 快捷方式 → 选择 “属性” → 在 “目标” 栏中查看旧版本安装路径。
安装新版本时，在安装向导中选择与旧版本完全相同的目录路径，覆盖旧版本文件。
2. 避免同时安装多个版本
若已安装多个版本，可以卸载重复版本：通过系统 “控制面板 - 程序和功能” 卸载非当前使用的版本。
3. 验证路径是否正确
若新版本安装后还是路径显示异常，建议彻底卸载后重新安装。

