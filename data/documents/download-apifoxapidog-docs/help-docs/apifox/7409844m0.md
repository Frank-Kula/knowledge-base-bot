# 为什么使用数据库步骤查看时间数据时，与 Navicat 之类的工具显示格式不一致？

不同工具对日期时间的显示和处理方式不同，导致了显示差异：
- Apifox 中 SQL 查询会严格按照数据库存储的原始类型（如 DATETIME、TIMESTAMP）和值来显示，基本不做额外美化
- Navicat 则会按自身逻辑对数据进行友好化展示，比如日期时间类型可能只显示日期、隐藏时间，让用户看得更直观

可以根据需要，使用自定义脚本步骤将执行 SQL 后获取的时间。

