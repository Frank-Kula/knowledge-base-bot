# 为什么linux系统在安装cli后不能直接使用

在系统上全局安装完之后 Apifox CLI

需要在 Linux 系统中创建一个符号链接（Symbolic Link），让系统全局范围内都能直接通过 apifox 命令运行该程序。
```js
[root@bogon bin]# ln -s /home/bjz/node-v22.14.0-linux-x64/bin/apifox /usr/local/bin/apifox
```
