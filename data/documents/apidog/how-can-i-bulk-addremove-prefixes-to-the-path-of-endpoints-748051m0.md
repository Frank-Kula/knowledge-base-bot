# How can I bulk add/remove prefixes to the path of endpoints?

It is not possible to process path changes through bulk operations. You can export the file in Apidog format from settings, perform text-based batch find-and-replace, and then re-import the file back into Apidog.
