# Why do I see the message "Couldn't resolve host" when sending a request?


1.   Need to check if the `Base URL` of the endpoint is missing "`/`".

2.   If the URL of the API contains an environment variable, check whether the value of the variable is incorrect.
