# 概述


`JSON Schema 扩展`指的是对标准的 JSON Schema 进行自定义扩展，以满足特定需求或提供额外的功能。JSON Schema 是一种用于验证 JSON 数据结构的标准，但在某些情况下，标准的 JSON Schema 功能可能不足以描述复杂的验证逻辑或其他特殊需求。通过扩展，开发者可以添加自定义的属性、规则或描述，以实现更灵活的 JSON 数据验证。



`Apifox JSON Schema 扩展`指的是 Apifox 提供的一些自定义扩展，用于增强标准 JSON Schema 的功能，满足特定的 API 开发和测试需求。
